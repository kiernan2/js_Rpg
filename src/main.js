let charaList = [] // Global variable remove later

const generateCharacter = () => {
  let chara = {};
  if (characterType === "Player")
    {}
  else if (characterType === "Monster")
    {}
  else if (characterType === "Ally")
    {}
}

const updateStat = (chara, stat, value) => {
  return ();
}